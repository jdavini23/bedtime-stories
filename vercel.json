{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "framework": "nextjs",
  "env": {},
  "regions": ["iad1"],
  "routes": [
    { "src": "/", "dest": "/" },
    { "src": "/api/generateStory", "dest": "/api/generateStory" }
  ]
}
