import { UserPreferencesLocal } from '@/types/preferences';

export const TEST_USER_ID = 'user_123';

export const TEST_PREFERENCES: UserPreferencesLocal = {
  theme: 'light',
  readingLevel: 'intermediate',
  storyLength: 'medium',
  generatedStories: 0,
};
